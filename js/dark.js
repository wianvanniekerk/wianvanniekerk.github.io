const options = {
    bottom: '3px',
    right: 'unset',
    left: '3px',
    time: '0.5s',
    mixColor: '#f0fff1',
    backgroundColor: '#fff',
    buttonColorDark: '#100f2c',
    buttonColorLight: '#fff',
    saveInCookies: true, 
    label: '🌓',
  }

  const darkmode = new Darkmode(options);
  darkmode.showWidget();
